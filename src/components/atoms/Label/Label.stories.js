import Label from './Label';

export default {
  title: 'Components/Atoms/Label',
  component: Label,
};

const Template = (args) => <Label {...args}>Email</Label>;

export const Default = Template.bind({});
